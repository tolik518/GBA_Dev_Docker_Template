version: '3.5'
services:
  dkp_compiler:
    image: ${VENDORNAME}/${PROJECTNAME}/${CONTAINERNAME}:dev
    container_name: ${CONTAINERNAME}
    volumes:
      - ${PWD}/code:/${USER}
      - ${PWD}/out:/out